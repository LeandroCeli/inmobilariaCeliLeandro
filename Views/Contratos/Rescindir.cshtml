@model inmobilariaCeli.Models.Contrato
@{
    ViewData["Title"] = "Rescindir contrato";
}

<h2>Rescindir contrato</h2>

<div class="alert alert-warning">
    <p>¿Estás seguro de que querés <strong>rescindir</strong> el siguiente contrato? Esta acción marcará el contrato como dado de baja y liberará el inmueble.</p>
</div>

<div class="mb-3">
    <strong>Inquilino:</strong> @Model.InquilinoNombre<br />
    <strong>Propiedad:</strong> @Model.PropiedadDireccion<br />
    <strong>Inicio:</strong> @Model.FechaInicio.ToShortDateString()<br />
    <strong>Fin:</strong> @Model.FechaFin.ToShortDateString()<br />
    <strong>Monto mensual:</strong> $@Model.MontoMensual<br />
    <strong>Depósito:</strong> $@Model.Deposito<br />
    <strong>Tipo de ocupación:</strong> @Model.TipoOcupacion
</div>

<form asp-action="RescindirConfirmado" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger">Confirmar rescisión</button>
    <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
</form>